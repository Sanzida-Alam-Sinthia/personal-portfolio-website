import React from 'react';
import { Container } from 'react-bootstrap';

const Blogs = () => {
    return (
        <Container className=''
            style={{ justifyContent: "center", padding: "10px" }}>
            <h2 style={{ justifyContent: "center", padding: "10px" }}>Coming Soon...</h2>
        </Container>

    );
};

export default Blogs;